import Layout from "../components/Layout";

const home = () => {
    return (
        <Layout>
            This is the main content written from the home page folder
        </Layout>
    );
}

export default home;